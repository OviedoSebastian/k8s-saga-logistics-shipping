apiVersion: apps/v1
kind: Deployment
metadata:
  name: label-service
  namespace: saga-shipping
  labels:
    app: label-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: label-service
  template:
    metadata:
      labels:
        app: label-service
    spec:
      containers:
      - name: label-service
        image: label-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5007
          name: http
        env:
        - name: SERVICE_NAME
          value: "label-service"
        - name: SERVICE_PORT
          value: "5002"
        - name: FAILURE_RATE
          value: "0.3"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5007
          initialDelaySeconds: 10
          periodSeconds: 10